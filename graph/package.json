{
  "name": "tornadolend",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pertsev/tornadolend",
    "create-local": "graph create --node http://localhost:8020/ pertsev/tornadolend",
    "remove-local": "graph remove --node http://localhost:8020/ pertsev/tornadolend",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 pertsev/tornadolend"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.12.0",
    "@graphprotocol/graph-ts": "0.11.0"
  }
}
